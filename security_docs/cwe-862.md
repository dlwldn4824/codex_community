# CWE-862: Missing Authorization

The product does not perform an authorization check when an actor attempts to access a resource or perform an action.

Authentication alone is insufficient. After identifying the caller, the server must verify that the caller's role is permitted to access the requested administrator resource. Missing this check can expose other users' private records.
